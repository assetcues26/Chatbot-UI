# Mirror of netlify.toml for humans. Netlify itself reads netlify.toml, not this file.
build:
  command: npm run build
  publish: dist
  environment:
    NODE_VERSION: "20"
    VITE_API_URL: "https://chatbot-backend-h6oj.onrender.com"

redirects:
  - from: "/*"
    to: /index.html
    status: 200
